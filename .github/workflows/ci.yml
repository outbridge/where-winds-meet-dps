name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Prettier
        run: pnpm run format:check
      - name: ESLint
        run: pnpm run lint
      - name: English-only guard
        env:
          LC_ALL: C.UTF-8
          LANG: C.UTF-8
        run: |
          hits=$(grep -rlIP '[\x{4e00}-\x{9fff}]' --exclude=zh.json --exclude=translations.ts src tests || true)
          if [ -n "$hits" ]; then
            echo "Chinese characters found in src/ or tests/ (English-only repo):" >&2
            echo "$hits" >&2
            exit 1
          fi
      - name: Typecheck
        run: pnpm run typecheck
      - name: Build
        run: pnpm run build
      - name: Tests
        run: pnpm test
